import React from 'react'
import { CalendarPlus, MapPin, Users, ArrowUpRight } from 'lucide-react'
import { CATEGORIES } from '../utils/calendar'
export default function PublicEventCard({event,onAdd,onOpen}){const cat=CATEGORIES[event.category];return <article className="public-event-card" style={{'--cat':cat.color,'--cat-2':cat.ribbon2}}><span className="life-ribbon ribbon-back"/><span className="life-ribbon ribbon-front"/><div className="public-card-topline"><span className="public-label">Public event</span><span className="public-category">{cat.label}</span></div><button className="public-title-button" onClick={()=>onOpen(event)}><span className="public-date">{event.displayDate} · {event.startTime}</span><span className="public-title">{event.title}</span></button><div className="public-host"><span className="avatar-letter">{event.owner.name.charAt(0)}</span><span><span>{event.owner.name}</span><small>{event.owner.role}</small></span></div><div className="public-meta"><span><MapPin size={13}/>{event.location}</span><span><Users size={13}/>{event.joined}{event.capacity?` / ${event.capacity}`:''} joined</span></div><div className="public-actions"><button className="secondary-btn" onClick={()=>onOpen(event)}>View card <ArrowUpRight size={14}/></button><button className="primary-btn" onClick={()=>onAdd(event)}><CalendarPlus size={15}/> Add to my week</button></div></article>}
